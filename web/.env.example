# Base URL of the Scanner Engine running on Oracle Cloud.
# Server-side only — never prefix with NEXT_PUBLIC_, or the key below leaks.
SCANNER_URL=http://localhost:8000
SCANNER_API_KEY=

# Seconds to wait for a full scan before giving up.
SCANNER_TIMEOUT=45
