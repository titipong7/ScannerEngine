/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The scanner URL and API key are read server-side only (see app/api/scan).
  env: {},
};

export default nextConfig;
